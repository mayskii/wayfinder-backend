# Wayfinder Backend

This repository contains the backend for **Wayfinder**, a trip planning web application.

The backend is built with **Java Spring Boot** and **PostgreSQL**, providing:

- REST API endpoints for cities, attractions, routes, and user data  
- Backend caching to minimize external API calls  
- Route optimization service for efficient trip planning  

---

## Frontend

The frontend is maintained in a separate repository:  
[Wayfinder Frontend](# Wayfinder Backend

This repository contains the backend for **Wayfinder**, a trip planning web application.

The backend is built with **Java Spring Boot** and **PostgreSQL**, providing:

- REST API endpoints for cities, attractions, routes, and user data  
- Backend caching to minimize external API calls  
- Route optimization service for efficient trip planning  

---

## Frontend

The frontend is maintained in a separate repository:  
[Wayfinder Frontend](# Wayfinder Backend

This repository contains the backend for **Wayfinder**, a trip planning web application.

The backend is built with **Java Spring Boot** and **PostgreSQL**, providing:

- REST API endpoints for cities, attractions, routes, and user data  
- Backend caching to minimize external API calls  
- Route optimization service for efficient trip planning  

---

## Frontend

The frontend is maintained in a separate repository:  
[Wayfinder Frontend](https://github.com/mayskii/wayfinder-frontend)

---

## Setup

1. Create a PostgreSQL database and configure credentials in `application.properties`  
2. Start the backend server:  

```bash
cd wayfinder-backend
./mvnw spring-boot:run
```

The server exposes REST endpoints for the frontend to consume.  

---

## Dependencies

- Spring Boot  
- Spring Data JPA (Hibernate)  
- PostgreSQL  
- Maven  )

---

## Setup

1. Create a PostgreSQL database and configure credentials in `application.properties`  
2. Start the backend server:  

```bash
cd wayfinder-backend
./mvnw spring-boot:run
```

The server exposes REST endpoints for the frontend to consume.  

---

## Dependencies

- Spring Boot  
- Spring Data JPA (Hibernate)  
- PostgreSQL  
- Maven

---

## Setup

1. Create a PostgreSQL database and configure credentials in `application.properties`  
2. Start the backend server:  

```bash
cd wayfinder-backend
./mvnw spring-boot:run
```

The server exposes REST endpoints for the frontend to consume.  

---

## Dependencies

- Spring Boot  
- Spring Data JPA (Hibernate)  
- PostgreSQL  
- Maven  
